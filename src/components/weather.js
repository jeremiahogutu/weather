import React, {Component} from 'react';

class Weather extends Component {
    render() {
        return (
            <div>
                weather component
            </div>
        );
    }
}

export default Weather;